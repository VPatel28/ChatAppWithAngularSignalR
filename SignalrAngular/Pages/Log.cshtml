@page
@model SignalrAngular.Pages.LogModel

@{
    Layout = null;
}

<!DOCTYPE html>


<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
<table class="table">
    <thead>
        <tr>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.LogData) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.LogD)
            </td>
        </tr>
}
    </tbody>
</table>
</body>
</html>
